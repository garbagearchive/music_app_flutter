name: music_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  audioplayers: ^6.5.0
  audio_service: ^0.18.0 # Or the latest stable version
  flutter:
    sdk: flutter
  provider: ^6.1.5
  hive: ^2.2.3 # Or the latest stable version
  hive_flutter: ^1.1.0 # Or the latest stable version
  cupertino_icons: ^1.0.8
  flutter_facebook_auth: ^6.1.1
  intl: ^0.18.0
  image_picker: ^1.1.2
  shared_preferences: ^2.5.3
  http: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/audio/
  - assets/images/
